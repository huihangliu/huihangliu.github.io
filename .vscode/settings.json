{
    //Set terminal as ubuntu
    // Bash on Ubuntu (on Windows)
    "terminal.integrated.shell.windows": "C:\\Windows\\System32\\bash.exe" // this is the defalut wsl distro
    // This open a zsh (not bash, not tmux)
}